<!DOCTYPE html>
<html>

<head>
  <title>Bizzy</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <nav class="max-width notification is-primary is-radiusless">
    <div class=" columns is-centered is-vcentered">
      <div class="column is-one-quarter">
        <%=session[:user_id]%>
        <%= link_to "Bizzy", root_path, class: 'is-size-3 has-text-weight-boldutton'%>
      </div>
      <div class="column">
      </div>
      <%if logged_in%>
      <div class="column is-narrow">
        <p class='has-text-weight-semibold'><%=user_full_name%></p>
      </div>
      <div class="column is-narrow">
        <%= link_to "Log Out", session_path,method:'delete', class: 'button' %>
      </div>
      <%else%>
      <div class="column is-narrow">
        <%= link_to "Sign in", '/signin', class: 'button'%>
      </div>
      <div class="column is-narrow">
        <%= link_to "Sign up", '/signup', class: 'button' %>
      </div>
      <%end%>
    </div>
  </nav>

  <div class='container'>
    <%= yield %>
    <%User.all.each do |u|%>

    <%end%>
  </div>
</body>

</html>